# Sentinel upload hardening snippet.
#
# Include this inside the `server` block that fronts the FastAPI app so large
# request bodies are rejected before they reach Python.

client_max_body_size 5M;

location /api/upload/ {
    client_max_body_size 5M;
    proxy_request_buffering on;
    proxy_pass http://sentinel_api;
}
